export enum FilterByData {
  LAST_RELEASED = 'LAST_RELEASED',
  LAST_WEEK = 'LAST_WEEK',
}
export enum FilterByOrder {
  released = 'released',
  added = 'added',
  created = 'created',
  updated = 'updated',
  name = 'name',
  rating = 'rating',
}
