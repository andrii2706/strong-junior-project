import { ReplaceNullImgPipe } from './replace-null-img.pipe';

describe('ReplaceNullImgPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceNullImgPipe();
    expect(pipe).toBeTruthy();
  });
});
